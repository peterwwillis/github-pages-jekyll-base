source 'https://rubygems.org'

# We comment these out because the Dockerfile we use installs the right versions
#ruby ">= 2.3.0"
#gem "jekyll", ">= 3.7.4"
#gem 'github-pages', ">= 197", group: :jekyll_plugins

gem 'jekyll-sitemap'
